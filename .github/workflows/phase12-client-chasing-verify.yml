name: Phase 12 Client Chasing Verify

on:
  workflow_dispatch:
  push:
    branches:
      - phase12-client-chasing-verify
    paths-ignore:
      - docs/demo/client-chasing-proof/**

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      DIRECT_URL: ${{ secrets.DIRECT_URL }}
      APP_FIELD_ENCRYPTION_KEY: ${{ secrets.APP_FIELD_ENCRYPTION_KEY }}
      NEXTAUTH_SECRET: ${{ secrets.NEXTAUTH_SECRET }}
      CRON_SECRET: ${{ secrets.CRON_SECRET }}
      PRODUCTION_URL: https://aria-ai-agent-vert.vercel.app
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Secret presence
        run: |
          for name in DATABASE_URL DIRECT_URL APP_FIELD_ENCRYPTION_KEY NEXTAUTH_SECRET CRON_SECRET; do
            value="${!name:-}"
            if [ -n "$value" ]; then
              echo "$name: present"
            else
              echo "$name: missing"
              exit 1
            fi
          done

      - name: Install dependencies
        run: npm ci

      - name: Prisma validate
        run: npx prisma validate

      - name: Prisma generate
        run: npx prisma generate

      - name: Prisma migrate deploy
        run: npx prisma migrate deploy

      - name: Prisma migrate status
        run: npx prisma migrate status

      - name: TypeScript
        run: npx tsc --noEmit

      - name: Build
        run: npm run build

      - name: Client chasing readiness
        run: npx tsx scripts/client-chasing-readiness.ts

      - name: Push readiness
        run: npx tsx scripts/push-notification-readiness.ts

      - name: Platform admin readiness
        run: npx tsx scripts/platform-admin-readiness.ts

      - name: Production security validation
        run: npx tsx scripts/production-security-validation.ts

      - name: Capture client chasing proof screenshots
        run: |
          set -o pipefail
          if ! npx tsx scripts/capture-client-chasing-screenshots.ts > phase12-screenshot-capture.log 2>&1; then
            tail -n 80 phase12-screenshot-capture.log
            message="$(tail -n 20 phase12-screenshot-capture.log | tr '\n' ' ' | sed -e 's/%/%25/g' -e 's/\r//g' -e 's/:/%3A/g' -e 's/,/%2C/g')"
            echo "::error title=Phase 12 screenshot capture failed::$message"
            exit 1
          fi

      - name: Commit proof screenshots
        run: |
          if [ -n "$(git status --short docs/demo/client-chasing-proof)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add docs/demo/client-chasing-proof
            git commit -m "Add Phase 12 proof screenshots"
            git push origin HEAD:phase12-client-chasing-verify
          else
            echo "No screenshot changes to commit."
          fi
